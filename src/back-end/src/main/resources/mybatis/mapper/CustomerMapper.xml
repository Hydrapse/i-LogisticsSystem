<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tcsquad.ilogistics.mapper.CustomerMapper">
    <select id="getCustomers" resultType="Customer">
        select
             customerid, cname, email, tel, addr, district, city, province
        from customer
    </select>
    <select id="getCustomerByCustomerId" resultType="Customer">
        select
             customerid as customerId, cname, email, tel, addr, district, city, province
        from customer
        where customerid = #{customerId}
    </select>
    <update id="updateCustomer" parameterType="Customer">
        update customer
        set
             email = #{email},
             tel = #{tel},
             addr = #{addr}
             district = #{district},
             city = #{city},
             province = #{province}
        where customerid = #{customerId}
    </update>
</mapper>