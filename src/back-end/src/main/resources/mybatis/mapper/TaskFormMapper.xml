<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tcsquad.ilogistics.mapper.TaskFormMapper">
    <select id="getTaskFormsByOrderId" resultType="TaskForm">
        select
            taskid,
            orderid,
            subsiteid,
            shipname,
            shiptel,
            shippro,
            shipcity,
            shipdis,
            billname,
            billtel,
            billpro,
            billcity,
            billdis,
            courier,
            totalprice,
            status,
            note
        from taskfrom
    </select>
    <
</mapper>